apiVersion: v1
kind: Service
metadata:
  name: {{ name }}-svc
  labels:
    app: {{ name }}
    type: letsencrypt
spec:
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: {{ name }}
    type: letsencrypt